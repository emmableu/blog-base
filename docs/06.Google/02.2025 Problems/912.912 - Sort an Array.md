---
title: 912 - Sort an Array
date: 2025-06-04 21:13:27
permalink: /pages/792a7c/
categories:
  - Google
  - 2025 Problems
tags:
  - tree
author:
  name: emmableu
---
[912. 排序数组](https://leetcode.cn/problems/sort-an-array/)

给你一个整数数组 `nums`，请你将该数组升序排列。

你必须在 **不使用任何内置函数** 的情况下解决问题，时间复杂度为 `O(nlog(n))`，并且空间复杂度尽可能小。


输入：nums = [5,2,3,1]
输出：[1,2,3,5]

**示例 2：**

输入：nums = [5,1,1,2,0,0]
输出：[0,0,1,1,2,5]

**提示：**

- `1 <= nums.length <= 5 * 104`
- `-5 * 104 <= nums[i] <= 5 * 104`


## Solution 1: quick sort 快速排序

lomuto partition: https://dlang.org/blog/2020/05/14/lomutos-comeback/ 

pivot: the last index
index that swaps value: i and j.
- `[0, i)`: smaller than pivot
- `[i, j)`: bigger than pivot
- `[j, pivot)`: unscanned



<img src="https://raw.githubusercontent.com/emmableu/image/master/202506042227480.png" alt="Visualization" style="width: 300px;">


#### Step 1: implement function with placeholders
```python
class Solution:
	def sortArray(self, nums: List[int]) -> List[int]:
		self.sort(nums, 0, len(nums) - 1)
		
	def sort(self, nums: List[int], lo: int, hi: int)
		# TODO: partition, returns p, make sure nums[:p-1] is smaller than p, nums[p+1:] is bigger than p
		self.sort(nums, lo, p - 1)
		self.sort(nums, p + 1, hi)
		
```

#### Step 2: implement and test partition
```python
def partition(nums, lo, hi):  
    pivot = hi
    i = lo
    for j in range(i, pivot):  
       if nums[j] < nums[pivot]:  
          nums[j], nums[i] = nums[i], nums[j]  
          i += 1  
    # if i is 0 here, it means a. [0, pivot-1] are all bigger than pivot, 
    # OR, b. pivot = i + 1 so it did not go into the loop.
    # in situation a, we always need to exchange pivot and i, 
    # in situation b, we need to exchange only if nums[pivot] < nums[i]
    # so, to consider both situation, we check first. 
    if nums[pivot] < nums[i]:
	    nums[pivot], nums[i] = nums[i], nums[pivot]  
    return i
  
nums = [4,3,1,2]  
# => [1,3,4,2]  
# => [1,2,4,3]  
partition(nums, 0, 3)  
print(nums)  
assert nums == [1,2,4,3]
```

### Step 3: add partition into sort
```python
class Solution:
	def sortArray(self, nums: List[int]) -> List[int]:
		self.sort(nums, 0, len(nums) - 1)
		return nums
	
	def partition(self, nums, lo, hi):  
	    pivot = hi
	    i = lo
	    for j in range(i, pivot):  
	       if nums[j] < nums[pivot]:  
	          nums[j], nums[i] = nums[i], nums[j]  
	          i += 1 
	    if nums[pivot] < nums[i]:
		    nums[pivot], nums[i] = nums[i], nums[pivot]  
	    return i
    
	def sort(self, nums: List[int], lo: int, hi: int):
		if hi <= lo:
			return
		p = self.partition(nums, lo, hi)
		self.sort(nums, lo, p - 1)
		self.sort(nums, p + 1, hi)
```


### Step 4 (optional): Add test
```python
def test_sort():  
    original = list(range(10))  
    for _ in range(100):  
        nums = random.sample(original, len(original))  
        solution = Solution()  
        sorted = solution.sortArray(nums)  
        assert original == sorted
```